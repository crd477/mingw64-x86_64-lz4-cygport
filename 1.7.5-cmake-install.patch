--- origsrc/lz4-1.7.5/contrib/cmake_unofficial/CMakeLists.txt	2017-01-02 23:09:52.000000000 -0600
+++ src/lz4-1.7.5/contrib/cmake_unofficial/CMakeLists.txt	2017-09-03 17:59:27.423929000 -0500
@@ -169,6 +169,7 @@ if(NOT LZ4_BUNDLED_MODE)
     RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}")
   install(TARGETS ${LZ4_LIBRARIES_BUILT}
     LIBRARY DESTINATION "${CMAKE_INSTALL_LIBDIR}"
+    RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
     ARCHIVE DESTINATION "${CMAKE_INSTALL_LIBDIR}")
   install(FILES
     "${LZ4_LIB_SOURCE_DIR}/lz4.h"
